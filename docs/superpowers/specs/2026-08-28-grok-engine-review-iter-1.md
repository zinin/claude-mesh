# Review Iteration 1 — 2026-08-28 21:20

## Источник

- Design: `docs/superpowers/specs/2026-08-28-grok-engine-design.md`
- Plan: `docs/superpowers/plans/2026-08-28-grok-engine.md`
- Review agents: claude:opus, claude:fable, codex, ext-claude(deepseek/v4-pro), ext-claude(ollama/kimi), ext-claude(ollama/minimax). zai/glm выбыл — три попытки watchdog, каждая `is_error: true` с `Prompt is too long`
- Merged output: `docs/superpowers/specs/2026-08-28-grok-engine-review-merged-iter-1.md`
- Режим: autodecide (вызван пользователем на первом спорном вопросе)

## Замечания

### [CRITICAL-1] Q1 переполняется: 5 опций при жёстком лимите AskUserQuestion в 4

**Источник:** claude:opus (Critical 1), claude:fable (C1), codex (Critical 2), deepseek/v4-pro (Critical 4), ollama/kimi (Critical 1) — 5 из 6 независимых ревьюеров; плюс воспроизведено оркестратором (лимит и четыре существующие опции проверены на живых файлах). Самая сильная находка итерации.
**Статус:** Решено автоматически (autodecide)
**Ответ:** Вариант A (autodecide)
**Уверенность:** уверенно
**Коммит:** 31c392b
**Действие:** Q1 сворачивает codex/gemini/grok в одну опцию «внешние CLI» с последующей страницей выбора движка на существующей пагинации; страница пропускается при единственном настроенном движке.

---

### [CRITICAL-2] `assert_eq_str` не существует — Tasks 2 и 3 дадут ложно-зелёный прогон

**Источник:** claude:opus (Critical 2), claude:fable (C3), deepseek/v4-pro (Critical 1), ollama/kimi (Critical 3) — 4 независимых; плюс воспроизведено оркестратором (grep по skills/ и commands/ пуст).
**Статус:** Автоисправлено
**Ответ:** В Task 2 добавлен Step 2a: хелпер assert_eq_str определяется до первого использования, с проверкой grep -c. Без него сьюта печатала бы «0 failed», не выполнив ни одной проверки.
**Действие:** В Task 2 добавлен Step 2a: хелпер assert_eq_str определяется до первого использования, с проверкой grep -c. Без него сьюта печатала бы «0 failed», не выполнив ни одной проверки.

---

### [CRITICAL-3] Task 10 оставляет `preflight-env.sh` падающим и сьюту красной: `HAS_GROK` unbound + row-order assert

**Источник:** (а) claude:opus (Critical 3), codex (Critical 4), deepseek/v4-pro (Critical 2); (б) codex (Critical 4), deepseek/v4-pro (Critical 3), ollama/kimi (Critical 4). Обе воспроизведены оркестратором на живом коде.
**Статус:** Автоисправлено
**Ответ:** Task 10 получил Step 0 (правка эталонной строки порядка row'ов) и явное требование объявить HAS_GROK=0 на строке 186, вне OK-ветки, с проверкой на пути run_probe none.
**Действие:** Task 10 получил Step 0 (правка эталонной строки порядка row'ов) и явное требование объявить HAS_GROK=0 на строке 186, вне OK-ветки, с проверкой на пути run_probe none.

---

### [CRITICAL-4] Таблица «Verified facts» дизайна расходится с живым CLI, а план фиксирует её как Global Constraints

**Источник:** claude:opus (Critical 4 + приложение A), claude:fable (C4) — 2 независимых; полностью воспроизведено оркестратором (это установленный факт, а не заявление). Один из FLIP-прогонов (opus) видел ещё третий, более короткий каталог — то есть каталог у пользователя меняется во времени.
**Статус:** Автоисправлено
**Ответ:** Таблица «Verified facts» переписана как снимок машины: 20 моделей, дефолт codex-sol, effort low; пятый уровень max добавлен в валидатор, Global Constraints и README; зафиксировано, что --effort — реальный алиас.
**Действие:** Таблица «Verified facts» переписана как снимок машины: 20 моделей, дефолт codex-sol, effort low; пятый уровень max добавлен в валидатор, Global Constraints и README; зафиксировано, что --effort — реальный алиас.

---

### [CRITICAL-5] Task 12 диспатчит не того агента: grok в design-review отревьюит git-diff вместо документов

**Источник:** claude:fable (C2), codex (Critical 1), ollama/kimi (Critical 2) — 3 независимых. Примечательно: `claude:opus` эту находку **пропустил**.
**Статус:** Автоисправлено
**Ответ:** Task 12 диспатчит claude-mesh:grok-executor во всех трёх местах; выписан шаблон диспатча в форме ext-claude; секция merged-файла переименована; удалена ссылка на несуществующий spec loop.
**Действие:** Task 12 диспатчит claude-mesh:grok-executor во всех трёх местах; выписан шаблон диспатча в форме ext-claude; секция merged-файла переименована; удалена ссылка на несуществующий spec loop.

---

### [CRITICAL-6] Сломанная секция `grok:` роняет весь оркестратор, а не свою строку — регресс класса «ultra incident»

**Источник:** claude:opus (Critical 5 + отдельный Concern), claude:fable (N4), codex (Critical 8), deepseek/v4-pro (Question 3) — 4 независимых. Один FLIP-прогон воспроизвёл цепочку эмпирически на существующем аналоге `claude: false`.
**Статус:** Решено автоматически (autodecide)
**Ответ:** Вариант 1 (autodecide)
**Уверенность:** уверенно
**Коммит:** cea4c47
**Действие:** Проверка каталога на пути validate_defaults стала ленивой: безусловный type-gate, полная проверка — только когда пресет ссылается на grok. Оркестратор деградирует grok вместо выхода. Фикстура получила блок defaults:.

---

### [CRITICAL-7] Task 13 Step 1 противоречит сам себе и вписывает ложное утверждение в byte-pinned регион

**Источник:** claude:opus (Critical 6), claude:fable (N7), codex (Concern), ollama/kimi (Concern) — 4 независимых; воспроизведено оркестратором (лимиты регионов и пины проверены).
**Статус:** Автоисправлено
**Ответ:** Task 13 Step 1 переписан: правка внутри PREFLIGHT-региона делается одинаково в обоих файлах без изменения числа строк, а утверждение про heuristic разведено для grok (его probe — именно auth-check).
**Действие:** Task 13 Step 1 переписан: правка внутри PREFLIGHT-региона делается одинаково в обоих файлах без изменения числа строк, а утверждение про heuristic разведено для grok (его probe — именно auth-check).

---

### [CRITICAL-8] Санитизация `MODEL` в grok-exec: обход `runs/grok/`, тихое обрезание, расхождение пути и `-m`

**Источник:** codex (Critical 3, с эмпирической проверкой), ollama/kimi (Concern) — 2 независимых; плюс оба FLIP-прогона opus. Молчаливая перезапись значения здесь хуже отказа: валидатор конфига уже гарантирует charset для review-пути.
**Статус:** Автоисправлено
**Ответ:** MODEL отвергается, а не переписывается: regex с якорем вместо tr -cd, значение персистится в .model, оба последующих блока читают файл вместо повторного разворота плейсхолдера.
**Действие:** MODEL отвергается, а не переписывается: regex с якорем вместо tr -cd, значение персистится в .model, оба последующих блока читают файл вместо повторного разворота плейсхолдера.

---

### [CRITICAL-9] `extract-result.py` теряет сообщение об ошибке grok — `output.txt` получает `API Error: {}`

**Источник:** claude:fable (N8), codex (Critical 7) — 2 независимых; **воспроизведено оркестратором** — это установленный факт, а не заявление.
**Статус:** Автоисправлено
**Ответ:** Task 6 получил Step 0: аддитивная правка extract-result.py на верхнеуровневый .message плюс регрессия на обе формы; исправлены оба ложных утверждения (дизайн §2 и комментарий Task 6).
**Действие:** Task 6 получил Step 0: аддитивная правка extract-result.py на верхнеуровневый .message плюс регрессия на обе формы; исправлены оба ложных утверждения (дизайн §2 и комментарий Task 6).

---

### [CRITICAL-10] Design-review Step 5.4 не покрыт: grok молча исчезает на итерациях 2..N плюс четыре недостающие правки

**Источник:** claude:opus (два Concern), claude:fable (N3 + C2-хвост), codex (Critical 6), ollama/kimi (Concern), ollama/minimax (Critical 3) — 5 из 6 независимых; плюс оба FLIP-прогона.
**Статус:** Автоисправлено
**Ответ:** Task 12 получил Step 3b с пятью правками Step 5.4 (SELECTED_GROK_MODELS в строке :381, разворот на странице подтверждения, клауза «Перевыбрать», строка про пустой выбор, обязательный пустой биндинг) и механическую проверку; исправлена адресация «Step 5.4 (dispatch)».
**Действие:** Task 12 получил Step 3b с пятью правками Step 5.4 (SELECTED_GROK_MODELS в строке :381, разворот на странице подтверждения, клауза «Перевыбрать», строка про пустой выбор, обязательный пустой биндинг) и механическую проверку; исправлена адресация «Step 5.4 (dispatch)».

---

### [CONCERN-1] `SUMMARY defaults` не разворачивает grok — probe начнёт ложно ругаться на `default`-режим

**Источник:** claude:opus (Concern), codex (Critical 4, третий буллет), deepseek/v4-pro (Concern 5) — 3 независимых; плюс FLIP-прогон.
**Статус:** Автоисправлено
**Ответ:** Task 10 обязан разворачивать grok на строке SUMMARY defaults тем же jq, что и claude; прямо запрещено вместо этого добавлять исключение в тест-хелпер.
**Действие:** Task 10 обязан разворачивать grok на строке SUMMARY defaults тем же jq, что и claude; прямо запрещено вместо этого добавлять исключение в тест-хелпер.

---

### [CONCERN-2] Step 5b (team mode) не покрыт — grok-ревьюеры не будут порождены

**Источник:** claude:opus (Concern), claude:fable (N9), codex (Critical 6, первый буллет) — 3 независимых; плюс FLIP N4.
**Статус:** Автоисправлено
**Ответ:** Task 11 получил Step 5b: team-режим порождает задачу на каждую пару grok:<model>.
**Действие:** Task 11 получил Step 5b: team-режим порождает задачу на каждую пару grok:<model>.

---

### [CONCERN-3] Устаревшие перечисления движков в семи с лишним местах обоих оркестраторов и в шапке guard'а

**Источник:** claude:opus (два Concern), claude:fable (N9), codex (Concern), deepseek/v4-pro (Concern 9) — 4 независимых; FLIP N4 даёт самый полный список строк.
**Статус:** Автоисправлено
**Ответ:** Там же — сквозной свип перечислений «codex / gemini / ext-claude» с проверкой grep и правилом, что исторические утверждения не трогаются.
**Действие:** Там же — сквозной свип перечислений «codex / gemini / ext-claude» с проверкой grep и правилом, что исторические утверждения не трогаются.

---

### [CONCERN-4] Default-ветка grok-exec: потерян load-bearing guard и цикл может вернуть 1 на успешном ране

**Источник:** claude:opus (Concern), ollama/kimi (Concern) — 2 независимых; плюс FLIP K9. Подтверждено чтением плановых строк 1000-1005 и `gemini-exec/SKILL.md:230`.
**Статус:** Автоисправлено
**Ответ:** Восстановлена форма if/then вместо конструкции `test && echo` с объяснением, почему SC2015-форма роняет цикл на здоровом ране.
**Действие:** Восстановлена форма if/then вместо конструкции `test && echo` с объяснением, почему SC2015-форма роняет цикл на здоровом ране.

---

### [CONCERN-5] Supervised-блок теряет три страховки ext-claude, а обещанная дизайном проверка терминального события на боевом пути не срабатывает никогда

**Источник:** claude:opus (Concern), codex (Critical 7, дополнительные буллеты) — 2 независимых; подтверждено чтением плановых строк 1085-1100.
**Статус:** Автоисправлено
**Ответ:** Task 6 получил Step 9b: четыре страховки ext-claude в supervised-блоке — толерантный вызов экстрактора, empty-output guard, копирование stderr.txt/raw.json в корень, перенос проверки терминального события в общий хвост.
**Действие:** Task 6 получил Step 9b: четыре страховки ext-claude в supervised-блоке — толерантный вызов экстрактора, empty-output guard, копирование stderr.txt/raw.json в корень, перенос проверки терминального события в общий хвост.

---

### [CONCERN-6] `HTTP_TIMEOUT` = 5 c на `grok models` — неизмеренный бюджет

**Источник:** claude:opus (Concern), claude:fable (N6), deepseek/v4-pro (Suggestion) — 3 независимых; плюс FLIP K3.
**Статус:** Автоисправлено
**Ответ:** Замерено: grok models — 1.0–1.2 с на тёплом старте, четырёхкратный запас к HTTP_TIMEOUT=5. Замер записан в дизайн вместе с условием, при котором понадобится отдельный PREFLIGHT_CLI_TIMEOUT.
**Действие:** Замерено: grok models — 1.0–1.2 с на тёплом старте, четырёхкратный запас к HTTP_TIMEOUT=5. Замер записан в дизайн вместе с условием, при котором понадобится отдельный PREFLIGHT_CLI_TIMEOUT.

---

### [CONCERN-7] `reasoning_effort` в самом CLI — per-model свойство, и 17 из 20 моделей каталога его не объявляют

**Источник:** claude:opus (Concern) — 1 независимый, но **подтверждено первичными измерениями оркестратора** (структура `~/.grok/config.toml` воспроизведена).
**Статус:** Автоисправлено
**Ответ:** Задокументировано: reasoning_effort в CLI — per-model свойство; плагин не предсказывает поддержку, CLI отвергает что не примет. Названа команда, закрывающая вопрос замером.
**Действие:** Задокументировано: reasoning_effort в CLI — per-model свойство; плагин не предсказывает поддержку, CLI отвергает что не примет. Названа команда, закрывающая вопрос замером.

---

### [CONCERN-8] `grep -q '"type":"result"'` даёт ложноположительный результат

**Источник:** claude:opus (Concern) — 1 независимый; логика проверяема по коду.
**Статус:** Автоисправлено
**Ответ:** Проверка терминального события переведена с grep на jq в обоих местах: подстрока встречается в tool_result и в прозе модели.
**Действие:** Проверка терминального события переведена с grep на jq в обоих местах: подстрока встречается в tool_result и в прозе модели.

---

### [CONCERN-9] Кросс-движковая атрибуция: одна модель за двумя транспортами считается двумя подтверждениями

**Источник:** claude:opus (Concern) — 1 независимый, но опирается на установленный оркестратором факт (каталог из 20 моделей).
**Статус:** Автоисправлено
**Ответ:** В дизайн §3 добавлена оговорка: grok-модель, дублирующая настроенного ext-claude провайдера, не является независимым подтверждением; плагин это не детектирует.
**Действие:** В дизайн §3 добавлена оговорка: grok-модель, дублирующая настроенного ext-claude провайдера, не является независимым подтверждением; плагин это не детектирует.

---

### [CONCERN-10] Покрытие guard'а неполно относительно обещания дизайна §5: нет KILLED, нет grok-ветки FLOOR_NOTE, нет позитивного assert для DEGRADED

**Источник:** claude:fable (N5), codex (Concern), ollama/kimi (Concern), ollama/minimax (Critical 2) — 4 независимых; плюс FLIP N2. (Оговорка ollama/minimax частично неверна — `assert_no_match` как раз ловит подстановку ext-claude-текста; пробел в отсутствии позитивного assert реальный.)
**Статус:** Автоисправлено
**Ответ:** Task 8 получил кейсы grok KILLED и grok STALLED-floor, а тест DEGRADED — позитивный assert на grok-текст в дополнение к assert_no_match.
**Действие:** Task 8 получил кейсы grok KILLED и grok STALLED-floor, а тест DEGRADED — позитивный assert на grok-текст в дополнение к assert_no_match.

---

### [CONCERN-11] Путь DEGRADED для grok не подтверждён ни одним измерением

**Источник:** FLIP-прогон opus (K4), ollama/minimax (Concern про bypassPermissions/Read) — фактически один голос opus плюс частичное совпадение.
**Статус:** Автоисправлено
**Ответ:** Формулировка дизайна смягчена до «eligible, not demonstrated»: ни один прогон не наблюдался с permission_denials, проверен классификатор, а не поведение CLI.
**Действие:** Формулировка дизайна смягчена до «eligible, not demonstrated»: ни один прогон не наблюдался с permission_denials, проверен классификатор, а не поведение CLI.

---

### [CONCERN-12] Task 9: номера Test 31 и Test 32 в `test-watch-runs.sh` уже заняты

**Источник:** claude:fable (N10), ollama/kimi (Concern) — 2 независимых; плюс FLIP N3/K1. (Проверка соседнего пина в Step 3 при этом корректна: Test 23 не ассертит точный текст stall-floor, только «600».)
**Статус:** Автоисправлено
**Ответ:** Номера тестов 31/32 заменены на 40/41 плюс проверка уникальности номеров в сьюте.
**Действие:** Номера тестов 31/32 заменены на 40/41 плюс проверка уникальности номеров в сьюте.

---

### [CONCERN-13] Task 5: счёт упоминаний `generate-md.sh` занижен, две из них — runtime-строки, а проверочный grep слишком узок

**Источник:** claude:opus (Suggestion), ollama/kimi (Concern) — 2 независимых; плюс FLIP K10/N8 с полным списком строк.
**Статус:** Автоисправлено
**Ответ:** Счёт упоминаний generate-md.sh исправлен на шесть, две из них названы runtime-строками, проверочный grep расширен.
**Действие:** Счёт упоминаний generate-md.sh исправлен на шесть, две из них названы runtime-строками, проверочный grep расширен.

---

### [CONCERN-14] Task 7 Step 4: `$PROMPT_FILE` не переживает между Bash-вызовами — «ambiguous redirect» в поставляемом скилле

**Источник:** claude:fable (N2) — 1 независимый, но опирается на явно документированное правило самого репозитория.
**Статус:** Автоисправлено
**Ответ:** Фенс из Task 7 Step 4 должен быть частью блока Step 3: PROMPT_FILE не переживает между Bash-вызовами и дал бы «ambiguous redirect» в поставляемом скилле.
**Действие:** Фенс из Task 7 Step 4 должен быть частью блока Step 3: PROMPT_FILE не переживает между Bash-вызовами и дал бы «ambiguous redirect» в поставляемом скилле.

---

### [CONCERN-15] `has_grok` — голый probe, а дизайн трактует его как гарантию непустого каталога

**Источник:** codex (Critical 8), deepseek/v4-pro (Concern 7) — 2 независимых; плюс FLIP N7. Решается вместе с CRITICAL-6.
**Статус:** Решено автоматически (autodecide)
**Ответ:** Вариант 1 (autodecide)
**Уверенность:** уверенно
**Коммит:** a580bb7
**Действие:** has_grok валидирует перед чтением, по образцу has_claude_models — инвариант дизайна стал истинным по построению.

---

### [CONCERN-16] Пустой эффективный roster: grok выбран, но ни одной модели не отмечено

**Источник:** codex (Critical 6, третий буллет + Question), ollama/minimax (Concern) — 2 независимых.
**Статус:** Автоисправлено
**Ответ:** Поведение пустого эффективного ростера определено явно в обоих оркестраторах, включая остановку при полностью пустом наборе.
**Действие:** Поведение пустого эффективного ростера определено явно в обоих оркестраторах, включая остановку при полностью пустом наборе.

---

### [CONCERN-17] `stdbuf`: три места расходятся, и на статически слинкованном бинаре это вообще no-op

**Источник:** claude:opus (Suggestion, с `file` на живом бинаре), deepseek/v4-pro (Concern 6), ollama/kimi (Concern) — 3 независимых; плюс FLIP K8/N6. Тип бинаря воспроизведён оркестратором.
**Статус:** Автоисправлено
**Ответ:** stdbuf убран из инвокации и из дизайна с объяснением: бинарь статический, stdbuf был бы no-op, поток и так небуферизован.
**Действие:** stdbuf убран из инвокации и из дизайна с объяснением: бинарь статический, stdbuf был бы no-op, поток и так небуферизован.

---

### [CONCERN-18] `test-command-sync.sh` не даёт и не может дать обещанного дизайном кросс-оркестраторного покрытия

**Источник:** claude:opus (Suggestion), claude:fable (Q4), codex (Critical 9, третий буллет) — 3 независимых; плюс FLIP N1.
**Статус:** Автоисправлено
**Ответ:** В дизайн §5 записано, что sync-сьюта сравнивает файлы друг с другом и слепа к одинаковой ошибке в обоих; добавлен статический orchestrator-contract блок.
**Действие:** В дизайн §5 записано, что sync-сьюта сравнивает файлы друг с другом и слепа к одинаковой ошибке в обоих; добавлен статический orchestrator-contract блок.

---

### [CONCERN-19] `| tail -3` скрывает rc тестов, а acceptance-цикл не накапливает падения

**Источник:** codex (Critical 9, второй буллет, с эмпирической проверкой) — 1 независимый; проверяемо тривиально.
**Статус:** Автоисправлено
**Ответ:** Все проверочные команды перестали глотать rc в пайпе; acceptance-цикл накапливает падения и выходит ненулевым.
**Действие:** Все проверочные команды перестали глотать rc в пайпе; acceptance-цикл накапливает падения и выходит ненулевым.

---

### [CONCERN-20] Task 1: baseline неполон, обещанной постоянной byte-identical регрессии нет, комментарии-обоснования теряются

**Источник:** ollama/minimax (Critical 1 — baseline), codex (Critical 9, первый буллет — golden + префикс die), claude:fable (S4), ollama/kimi (Concern — комментарии) — 4 независимых, каждый со своей частью; префикс `die()` воспроизведён оркестратором.
**Статус:** Автоисправлено
**Ответ:** Baseline Task 1 расширен с шести кейсов до девяти, исправлен несуществующий префикс «config-loader: ERROR:», добавлен постоянный golden-тест как Step 7.
**Действие:** Baseline Task 1 расширен с шести кейсов до девяти, исправлен несуществующий префикс «config-loader: ERROR:», добавлен постоянный golden-тест как Step 7.

---

### [CONCERN-21] `generate-md.sh` формально читает grok-формат, но теряет блоки

**Источник:** codex (Concern, с проверкой на фикстуре плана) — 1 независимый.
**Статус:** Автоисправлено
**Ответ:** Ограничение рендерера (теряет блоки в смешанном сообщении) зафиксировано в SKILL.md; правка для всех движков вынесена за рамки.
**Действие:** Ограничение рендерера (теряет блоки в смешанном сообщении) зафиксировано в SKILL.md; правка для всех движков вынесена за рамки.

---

### [CONCERN-22] Smoke-тест не проверяет то, ради чего задуман, и легко становится тихим SKIP

**Источник:** codex (Concern), ollama/minimax (два Concern) — 2 независимых; плюс FLIP K5.
**Статус:** Автоисправлено
**Ответ:** Smoke меряет рост raw.jsonl по двум снимкам вместо финального размера; отсутствие jq/python3 стало FAIL, а не тихим SKIP.
**Действие:** Smoke меряет рост raw.jsonl по двум снимкам вместо финального размера; отсутствие jq/python3 стало FAIL, а не тихим SKIP.

---

### [CONCERN-23] Ограничение «no skills» не доходит до design-review, и grok видит собственные скиллы claude-mesh

**Источник:** claude:fable (N1), ollama/kimi (Question) — 2 независимых.
**Статус:** Автоисправлено
**Ответ:** В Task 12 добавлено требование нести запрет на скиллы в PROMPT-блок grok-диспатчей: design-review минует grok-code-review и иначе теряет эту строку.
**Действие:** В Task 12 добавлено требование нести запрет на скиллы в PROMPT-блок grok-диспатчей: design-review минует grok-code-review и иначе теряет эту строку.

---

### [CONCERN-24] Ран без модели неатрибутируем и навсегда вне guard'а

**Источник:** claude:opus (Question 5), ollama/minimax (Concern), codex (Suggestion про namespace) — 3 независимых; плюс FLIP Q2/Q4.
**Статус:** Решено автоматически (autodecide)
**Ответ:** Вариант 2 (autodecide)
**Уверенность:** уверенно
**Коммит:** be81663
**Действие:** Ран без модели пишется в runs/grok/_default/<ts>-<task>/ — единая форма пути; инвариант «модель не спутать с раном» больше не нужен. Рендерер подписывается фактической моделью.

---

### [CONCERN-25] `SUMMARY available` объявляет модели из конфига, а не то, что вернул `grok models`

**Источник:** codex (Concern + Question) — 1 независимый; воспроизведено при чтении плана (шим действительно не печатает `Available models:`).
**Статус:** Автоисправлено
**Ответ:** Разведена семантика: grok OK — про CLI, grok:<model> в available — про конфиг; кросс-проверка с подпиской сознательно не делается.
**Действие:** Разведена семантика: grok OK — про CLI, grok:<model> в available — про конфиг; кросс-проверка с подпиской сознательно не делается.

---

### [SUGGESTION-1] Claude-специфичный пример `- "opus"` уезжает в grok-сообщения общего хелпера

**Источник:** claude:opus, claude:fable (S2), codex, deepseek/v4-pro (8) — 4 независимых; плюс FLIP S2.
**Статус:** Автоисправлено
**Ответ:** Запрещено хардкодить claude-специфичный пример в сообщениях общего хелпера.
**Действие:** Запрещено хардкодить claude-специфичный пример в сообщениях общего хелпера.

---

### [SUGGESTION-2] Пресетные циклы `claude_models` и `grok_models` остаются рукописными близнецами

**Источник:** claude:fable (S1), ollama/kimi (Concern), ollama/minimax (Suggestion) — 3 независимых.
**Статус:** Решено автоматически (autodecide)
**Ответ:** Вариант 2 (autodecide)
**Уверенность:** уверенно
**Коммит:** f237f3d
**Действие:** Пресетные петли остаются близнецами, связаны SYNC-маркерами: опасный страж уже общий, а слияние расширило бы замороженную поверхность.

---

### [SUGGESTION-3] Task 3 Step 4: якорь вставки указывает на два разных места

**Источник:** claude:opus, ollama/kimi — 2 независимых.
**Статус:** Автоисправлено
**Ответ:** Точка вставки в Task 3 Step 4 названа однозначно — сразу после закрывающего fi блока claude_models.
**Действие:** Точка вставки в Task 3 Step 4 названа однозначно — сразу после закрывающего fi блока claude_models.

---

### [SUGGESTION-4] `cli_row` `$5` без кавычек: word splitting плюс глоббинг как публичный контракт

**Источник:** claude:opus, ollama/minimax — 2 независимых.
**Статус:** Автоисправлено
**Ответ:** Word splitting у пятого аргумента задокументирован как публичный контракт cli_row; добавлены set -f/set +f с восстановлением на обеих ветках.
**Действие:** Word splitting у пятого аргумента задокументирован как публичный контракт cli_row; добавлены set -f/set +f с восстановлением на обеих ветках.

---

### [SUGGESTION-5] README troubleshooting: «`grok models` lists them» — реальный вывод не такой

**Источник:** claude:opus — 1; воспроизведено оркестратором.
**Статус:** Автоисправлено
**Ответ:** Строка README про grok models переписана под реальную форму вывода.
**Действие:** Строка README про grok models переписана под реальную форму вывода.

---

### [SUGGESTION-6] Task 4 не гоняет `test-config-loader.sh`, хотя два теста читают `config.example.yaml`

**Источник:** claude:opus (Test 31), claude:fable (S6, называет Test 51), ollama/minimax — 3 независимых; оба номера верны, тесты разные (проверено).
**Статус:** Автоисправлено
**Ответ:** Task 4 прогоняет test-config-loader.sh (Tests 31/32/51 читают config.example.yaml) и проверяет строку preflight.
**Действие:** Task 4 прогоняет test-config-loader.sh (Tests 31/32/51 читают config.example.yaml) и проверяет строку preflight.

---

### [SUGGESTION-7] Бухгалтерия фикстур расходится: 11 / «9 siblings» / 8 созданных

**Источник:** claude:fable (S7), ollama/kimi (мелкие расхождения, п. a) — 2 независимых.
**Статус:** Автоисправлено
**Ответ:** Бухгалтерия фикстур сведена: 8 файлов, 3 кейса inline, три числа приведены в соответствие.
**Действие:** Бухгалтерия фикстур сведена: 8 файлов, 3 кейса inline, три числа приведены в соответствие.

---

### [SUGGESTION-8] Новому правилу «no fallback» не хватает `<!-- SYNC: -->` маркеров

**Источник:** claude:fable (S8) — 1.
**Статус:** Автоисправлено
**Ответ:** Правило «нет фолбэка» получило SYNC-маркеры на четыре места, зеркально claude-правилу.
**Действие:** Правило «нет фолбэка» получило SYNC-маркеры на четыре места, зеркально claude-правилу.

---

### [SUGGESTION-9] Мелкие дефекты текста и кода, правятся механически

**Источник:** разные ревьюеры по одному пункту; ни один не спорен.
**Статус:** Автоисправлено
**Ответ:** Полноширинная скобка U+FF09 убрана, echo с литеральным переводом строки заменён на printf, добавлен rc-guard на list-grok-models, сравнения приведены к арифметическим.
**Действие:** Полноширинная скобка U+FF09 убрана, echo с литеральным переводом строки заменён на printf, добавлен rc-guard на list-grok-models, сравнения приведены к арифметическим.

---

### [SUGGESTION-10] Task 14 Step 1 предлагает править пользовательский `config.yaml` вопреки Global Constraints

**Источник:** FLIP-прогон opus (Question 1) — один голос; ollama/minimax явно закрыл это как шум («шаг только печатает и проверяет»). Оба правы про свою половину.
**Статус:** Автоисправлено
**Ответ:** Task 14 Step 1 переформулирован: конфиг правит только пользователь, команды шага лишь печатают и проверяют.
**Действие:** Task 14 Step 1 переформулирован: конфиг правит только пользователь, команды шага лишь печатают и проверяют.

---

### [SUGGESTION-11] `reasoning_effort: ""` у grok — `die`, а у codex пустая строка просто пропускается

**Источник:** deepseek/v4-pro (Suggestion) — 1.
**Статус:** Автоисправлено
**Ответ:** Пустая строка в reasoning_effort приравнена к «ключ не задан», как у codex.
**Действие:** Пустая строка в reasoning_effort приравнена к «ключ не задан», как у codex.

---

### [SUGGESTION-12] Ветка `ext-claude|grok` содержит ещё два ложных для grok места, кроме двух названных планом

**Источник:** deepseek/v4-pro (9), плюс FLIP S6 — 1 независимый + FLIP. (Для ext-claude формулировка про DSML неточна уже сегодня, так что это унаследованный, а не новый долг.)
**Статус:** Автоисправлено
**Ответ:** Предписан свип всей ветки ext-claude|grok на engine-специфичные формулировки, а не только двух названных мест.
**Действие:** Предписан свип всей ветки ext-claude|grok на engine-специфичные формулировки, а не только двух названных мест.

---

### [SUGGESTION-13] `LOADER_SUBCMDS` в sync-тесте не получает новые подкоманды

**Источник:** codex (Concern) — 1.
**Статус:** Автоисправлено
**Ответ:** LOADER_SUBCMDS в sync-сьюте расширен новыми подкомандами.
**Действие:** LOADER_SUBCMDS в sync-сьюте расширен новыми подкомандами.

---

### [SUGGESTION-14] Task 4 создаёт промежуточный коммит, предлагающий нерабочую конфигурацию

**Источник:** codex (Concern) — 1.
**Статус:** Автоисправлено
**Ответ:** grok попадает в builtin пресетов примера только в Task 13, когда всё за ними уже существует.
**Действие:** grok попадает в builtin пресетов примера только в Task 13, когда всё за ними уже существует.

---

### [SUGGESTION-15] Прочие мелочи Tasks 10/11/7, каждая от одного ревьюера

**Источник:** ollama/minimax — 1 ревьюер, три отдельных наблюдения.
**Статус:** Автоисправлено
**Ответ:** Недостижимая ветка add_avail grok помечена комментарием; прочие мелочи Tasks 10/11/7 отнесены к соответствующим задачам.
**Действие:** Недостижимая ветка add_avail grok помечена комментарием; прочие мелочи Tasks 10/11/7 отнесены к соответствующим задачам.

---

### [SUGGESTION-16] Не обсуждённые альтернативы дизайна

**Источник:** ollama/minimax (два альтернативных подхода), ollama/kimi (charset/timestamp) — 2 независимых.
**Статус:** Автоисправлено
**Ответ:** В дизайн добавлен абзац «Rejected alternatives» с обеими мотивировками.
**Действие:** В дизайн добавлен абзац «Rejected alternatives» с обеими мотивировками.

---

### [QUESTION-1] Что такое `grok.models` — модели grok или всё, что отдаёт CLI?

**Источник:** codex (Question), FLIP-прогон opus (Question 3) — открытый вопрос к автору дизайна.
**Статус:** Решено автоматически (autodecide)
**Ответ:** Вариант 1 (autodecide)
**Уверенность:** уверенно
**Коммит:** 7347a9b
**Действие:** grok.models = имена, которые принимает -m у этого CLI. Ограничение по семейству отвергнуто: запретило бы дефолт пользователя и всё равно не ловит дублирование. Оговорка про атрибуцию — в дизайн и в config.example.yaml.

---

### [QUESTION-2] `grok:` без значения (`null`) — отсутствующая секция или ошибка «models required»?

**Источник:** codex (Question) — 1.
**Статус:** Автоисправлено
**Ответ:** Ответ установлен прецедентом: секция со значением null трактуется как отсутствующая (codex: без значения даёт has_codex=0). Зафиксировано комментарием.
**Действие:** Ответ установлен прецедентом: секция со значением null трактуется как отсутствующая (codex: без значения даёт has_codex=0). Зафиксировано комментарием.

---

### [QUESTION-3] Нужен ли `--no-plan` при `--permission-mode bypassPermissions`?

**Источник:** FLIP-прогон opus (S6) — 1.
**Статус:** Автоисправлено
**Ответ:** Записано, что --no-plan ортогонален bypassPermissions: права управляют доступом инструментов, plan mode — тем, отвечает ли модель планом.
**Действие:** Записано, что --no-plan ортогонален bypassPermissions: права управляют доступом инструментов, plan mode — тем, отвечает ли модель планом.

---

### [QUESTION-4] Осознанно ли grok попадает в `config.example.yaml` «живым» — в обоих `builtin` и обоих `grok_models`?

**Источник:** FLIP-прогон opus (Question 2); связано с SUGGESTION-14.
**Статус:** Автоисправлено
**Ответ:** Решается вместе с SUGGESTION-14: пример не становится «живым» раньше Task 13.
**Действие:** Решается вместе с SUGGESTION-14: пример не становится «живым» раньше Task 13.

---

### [QUESTION-5] Порядок `BASE_BRANCH=<branch> MODEL=<entry> …` против «MODEL is REQUIRED on the first line»

**Источник:** codex (Critical 5 — считает блокером), ollama/kimi (Question — отмечает работающий прецедент) — 2 независимых, разные оценки серьёзности.
**Статус:** Автоисправлено
**Ответ:** MODEL обязан быть первой непустой строкой; порядок BASE_BRANCH/MODEL исправлен, потому что агент парсит начало строки на MODEL=.
**Действие:** MODEL обязан быть первой непустой строкой; порядок BASE_BRANCH/MODEL исправлен, потому что агент парсит начало строки на MODEL=.

---

### [QUESTION-6] Должен ли `defaults.<preset>.grok_models` быть обязателен в обе стороны?

**Источник:** FLIP-прогон opus (Question 4) — 1.
**Статус:** Отклонено
**Ответ:** Дизайн §1 уже задаёт правило в обе стороны: «mirrors the existing fail-closed rule that rejects claude_models without claude in builtin». Вопрос закрыт самим документом.
**Действие:** Правок не вносилось.

---

## Изменения в документах

| Файл | Изменение |
|------|-----------|
| `docs/superpowers/specs/2026-08-28-grok-engine-design.md` | Таблица фактов переписана как снимок машины (20 моделей, дефолт codex-sol, пять уровней effort); реструктуризация Q1; ленивая валидация секции grok; валидирующий has_grok; единая форма пути с namespace `_default`; семантика grok.models; оговорка про кросс-движковую атрибуцию; смягчено обещание DEGRADED; убран stdbuf; добавлены отвергнутые альтернативы |
| `docs/superpowers/plans/2026-08-28-grok-engine.md` | Хелпер assert_eq_str; HAS_GROK=0 и правка row-order assert; grok-executor вместо grok-code-reviewer в Task 12; пять правок Step 5.4; отвержение MODEL вместо переписывания; правка extract-result.py; паритет supervised-блока; переписан Task 13 Step 1; orchestrator-contract тест; сквозной захват rc; девять кейсов baseline и golden-тест |
| `docs/superpowers/specs/2026-08-28-grok-engine-review-merged-iter-1.md` | Новый: слитые отчёты шести ревьюеров плюс первичные улики оркестратора, 1498 строк |

## Статистика

- Всего замечаний: 57
- Автоисправлено (без обсуждения): 50
- Авто-применено после анализа: 0
- Обсуждено с пользователем: 0
- Решено автоматически (autodecide): 6
- из них под вопросом: 0
- Отклонено: 1
- Повторов (автоответ): 0
- Отложено (стоп): 0
- Пользователь сказал "стоп": Нет
- Агенты: claude:opus, claude:fable, codex, deepseek/v4-pro, ollama/kimi, ollama/minimax (zai/glm выбыл по контексту)
