# Review Iteration 1 — 2026-08-03 10:40

## Источник

- Design: `docs/superpowers/specs/2026-08-02-fresh-session-review-prompts-design.md`
- Plan: `docs/superpowers/plans/2026-08-02-fresh-session-review-prompts.md`
- Review agents: claude:opus, claude:fable, codex (gpt-5.6-sol, reasoning=max), ext-claude zai/glm, alibaba/qwen, deepseek/v4-pro, ollama/kimi, ollama/minimax — 8 из 8 завершились успешно (deepseek стартовал прогон на ~9 минут позже; ранний MISSING watcher'а был ложной смертью)
- Merged output: `docs/superpowers/specs/2026-08-02-fresh-session-review-prompts-review-merged-iter-1.md`
- Разбор и дедупликация: агент `claude-mesh:review-discussion` (~120 сырых находок → 57 уникальных)

## Замечания

### Блокирующие (Tier 1)

#### [B1] «SUMMARY available: claude» при отсутствующем конфиге — обещание, которое оркестраторы не выполняют

> Проба обещает claude при `config MISSING/INVALID`, но mesh-review Step 0/1 и mesh-design-review Step 5.0 при rc=2/rc=1 выходят до выбора ревьюеров; деградационная ветка промпта ведёт в тот же тупик.

**Источник:** claude:opus (C1), codex (Critical 1), ollama/kimi (Critical 1) — подтверждено по коду
**Статус:** Обсуждено с пользователем (вместе с Q1)
**Ответ:** Вариант A — понизить обещание пробы, не трогая замороженный зеркалируемый код оркестраторов
**Действие:** При `config` ≠ OK `SUMMARY available` печатает `—`, claude уходит в unavailable с причиной, после SUMMARY печатается `hint: cp config.example.yaml <data-dir>/config.yaml`; detail строки `builtin-claude` и rc=2 уточнены; decision 6, Error handling и деградационные ветки обоих шаблонов переписаны; тесты обновлены

#### [B2] `probe_provider` в command substitution — trap-гарантия удаления токен-файла не работает

**Источник:** claude:opus (C3), claude:fable (C1), codex (Critical 4) — подтверждено эмпирически
**Статус:** Автоисправлено
**Ответ:** Вердикт через глобалы `PROV_STATUS`/`PROV_DETAIL`, вызов отдельной командой (паттерн cli_row); INT/TERM → cleanup + exit 130/143 (прерывание — не вердикт)
**Действие:** План Task 2 переписан; дизайн Secrets + Global Constraints зафиксировали контракт

#### [B3] Тест-харнес: `RC`/`CFG_DIR` гибнут в subshell — ассерты «exit 0» и «env-файлы удалены» нефальсифицируемы

**Источник:** claude:opus (C2), codex (Critical 6) — подтверждено эмпирически
**Статус:** Автоисправлено
**Ответ:** Домашняя идиома `run()` из test-verify-delegation.sh:43 — присваивание глобалов, вызов отдельной командой
**Действие:** `run_probe` переписан, все call-sites переведены на standalone-вызовы

#### [B4] Сьюта негерметична: сценарии Task 1 шлют фикстурный токен в реальную сеть

**Источник:** claude:fable (C2), codex (Critical 7)
**Статус:** Автоисправлено
**Ответ:** Дефолтный curl-шим в `run_probe`, симметричный git-шиму
**Действие:** Блок `curlfast` добавлен в план Task 1

#### [B5] `$SCRATCH` не переживает границу задач — Task 6 Step 4 неисполним

**Источник:** claude:fable (C3), codex (Critical 8)
**Статус:** Автоисправлено
**Ответ:** Собственный `mktemp -d` в Task 6 Step 4 + запрет «Touch nothing outside $SCRATCH/repo» + критерий чистоты ИСХОДНОГО репозитория (субагент наследует cwd/HOME)
**Действие:** Task 5 Step 2 и Task 6 Step 4 дополнены, критериев прохождения стало 5

#### [B6] `config OK` (из `list-models`) не означает «конфиг годен для ревью»

**Источник:** claude:opus (K3), codex (Critical 2)
**Статус:** Авто-применено после анализа
**Ответ:** «Минимум оркестратора»: после rc=0 проба дополнительно гоняет `get-defaults design_review` и `get-flag dispatch_model`; codex/gemini сознательно вне гейта (битая опциональная секция валит свой ряд, не среду). Полный `validate` отвергнут как перестрел, сужение формулировки — как не-фикс
**Действие:** Ветка rc=0 расширена; сценарии `invalid-defaults-runmode` и `invalid-runtime-runmode` → `config INVALID`

#### [B7] `ollama-precheck.sh` игнорирует таймауты — бюджет «35 s» занижен, detail лжёт

**Источник:** все 8 ревьюеров
**Статус:** Автоисправлено
**Ответ:** Env-ручки `OLLAMA_PRECHECK_TRIES/ATTEMPT_TIMEOUT/TAGS_TIMEOUT` (дефолты сохраняют текущее поведение); проба ставит tries=1 и таймауты = `PREFLIGHT_HTTP_TIMEOUT`; честные числа в Cost
**Действие:** ollama-precheck.sh добавлен в File Structure как Modify; дизайн Invocation/Cost переписаны

#### [B8] Контракт `PREFLIGHT_CURL_BIN` не выполняется для провайдерных строк

**Источник:** codex, claude:fable (N6), qwen, deepseek, kimi, minimax
**Статус:** Автоисправлено
**Ответ:** Сужение: переменная правит только собственные пробы; при её нерезолве прекчеки не вызываются вовсе → провайдеры `UNKNOWN — no curl` (обещание строки curl стало правдой)
**Действие:** Гейт `HAVE_CURL` в цикле провайдеров + тест без PATH-хирургии

#### [B9] codex/gemini гейтятся на невалидирующий `has_*` → ложный OK; `claude-models` глотает смерть валидатора

**Источник:** zai/glm (Critical 1), claude:opus (K4), claude:fable (N1)
**Статус:** Автоисправлено
**Ответ:** Зеркалирование обоих гейтов UI: секция есть (`has_*`) → типизированный геттер (`get-codex`/`get-gemini`, rc≠0 → `INVALID` с реальной причиной) → CLI → сеть; `list-claude-models` rc-aware (INVALID достижим)
**Действие:** cli_row и claude-models-блок переписаны; фикстура `invalid-claude-scalar.yaml`; сценарий `broken-codex-valid-gemini`

#### [B10] `INVALID` смешивает «конфиг отвергнут» и «нет yq/jq»

**Источник:** claude:opus (C4), claude:fable (Q1), codex, deepseek (подтверждено эмпирически)
**Статус:** Автоисправлено
**Ответ:** `PREFLIGHT_YQ_BIN`/`PREFLIGHT_JQ_BIN`-проверки ДО loader'а: строки yq/jq (только при отсутствии) + `config UNKNOWN cannot evaluate`; loader при мёртвом тулчейне не вызывается; Dependencies дополнены
**Действие:** Секция toolchain в скелете + тест-сценарий

#### [B11] `exit 1` в discovery-блоке противоречит заявленной деградации

**Источник:** deepseek (Critical 4), ollama/kimi (Critical 2)
**Статус:** Автоисправлено
**Ответ:** `exit 0` с сообщением «expected degradation, NOT a broken environment»; `bash "$PF"` вместо `"$PF"` (shared-folder может терять exec-бит); текст деградации финализирован после B1
**Действие:** Блок обновлён в дизайне и обоих шаблонах плана

#### [B12] Discovery слеп к `--plugin-dir` и не называет активный плагин

**Источник:** opus (K1/S1/S2), fable (N3/S1), codex, qwen, minimax, deepseek (`sort -V`)
**Статус:** Обсуждено с пользователем (два уточняющих вопроса)
**Ответ:** B+C без A: рабочая копия первым кандидатом (главный мотив — иначе GREEN/ACCEPTANCE-прогоны плана до релиза не могут исполнить пробу вовсе) + строка `plugin <version> @ <path>`; команда-обёртка отклонена (рост API ради не-используемого dev-load); `--plugin-dir` — одна строка-оговорка в decision 2
**Действие:** Двухступенчатый discovery в decision 2 и шаблонах; identity-блок в скелете; ассерты и порядок строк обновлены

### Важные (Tier 2)

#### [I1] `provider:* OK` завышает доступность: claude/jq/bc/curl/python3 не проверяются
**Источник:** codex (Critical 3). **Статус:** Автоисправлено. **Действие:** строка `ext-claude-deps` (claude/bc/python3, только при пропаже; jq — у B10, curl — у B8), провайдеры деградируют в `MISSING` с перечнем, сеть не тратится

#### [I2] `do-plan` Step 7: finishing может случиться до внешнего ревью (+Q6)
**Источник:** codex (Critical 10). **Статус:** Авто-применено после анализа. **Ответ:** append с правилом порядка первым предложением (переписывание Step 7 противоречит собственному scope дизайна); запрет на время ревью включает и локальный merge — finishing удаляет ветку, фиксам некуда ложиться. **Действие:** абзац Task 8 Step 3 и дизайн-параграф переписаны

#### [I3] `TOPIC` из имени ветки ломается на реальных именах
**Источник:** codex (Critical 9), zai/glm, minimax. **Статус:** Автоисправлено. **Действие:** slug-правило (срез до последнего `/`, trailing-strip, `[a-z0-9-]`), detached HEAD → short sha, пустое → спросить

#### [I4] Нормализация TOPIC двусмысленна; «matching plan» не определён
**Источник:** claude:opus (K8). **Статус:** Автоисправлено. **Действие:** правило процитировано дословно (повторный strip, пример с двумя суффиксами); упорядоченный поиск плана `-implementation.md` → `-plan.md` → `<topic>.md` → newest

#### [I5] Порядок провайдеров — это порядок models, не providers
**Источник:** codex (Concern 1). **Статус:** Автоисправлено. **Действие:** формулировка «в порядке первого появления в models»; провайдер без моделей не пробуется — ему нечего предложить UI

#### [I6] Любой отказ `export` объявляется «token not configured»
**Источник:** opus (K9), codex (Concern 2), zai/glm (2). **Статус:** Автоисправлено. **Действие:** stderr捕жен; token/REPLACE_ME → MISSING, остальное → `UNKNOWN export refused: <причина>`; runtime теперь гейтится на строке config (B6)

#### [I7] У wording-микротеста нет позитивного критерия
**Источник:** minimax (Critical 2), fable (S6г). **Статус:** Автоисправлено. **Действие:** порог «кандидат ≥4/5 И контроль падает ≥2/3»; «выбросить блок» заменено на «вернуться к decision 3»

#### [I8] Верификация Task 6/7 неисполнима до переустановки плагина (+Q2)
**Источник:** claude:opus (K6). **Статус:** Авто-применено после анализа. **Ответ:** command-файлы — промпты: исполнять их шаги напрямую из рабочей копии (`/plugin update` невозможен до релиза, `--plugin-dir` не в workflow). **Действие:** шаги верификации и Testing-раздел переформулированы

#### [I9] Режим `default` обходит preflight
**Источник:** minimax (Critical 4 — подтверждён по коду), fable (Q2). **Статус:** Авто-применено после анализа. **Ответ:** B+C — строки `SUMMARY defaults <preset>` в орфографии UI + membership-правило в GO-секциях («default только если каждый участник пресета есть в available»); запрет default отвергнут — ломает реальный workflow пользователя. **Действие:** блок defaults в Task 4, правило в обоих шаблонах и дизайне

#### [I10] Односторонние SYNC-заметки; шаблоны-близнецы без взаимной связки
**Источник:** claude:fable (N4, N5). **Статус:** Автоисправлено. **Действие:** три встречных `<!-- SYNC -->` в SKILL.md (Task 8), взаимные sync-notes в обоих command-файлах; ожидание диффа Task 8 Step 2 обновлено

#### [I11] Деградация описана для отсутствующей пробы, но не для отсутствующих команд
**Источник:** alibaba/qwen (Concern 2 — самая содержательная её находка). **Статус:** Автоисправлено. **Действие:** Step 15 fallback на `continue-plan-fresh-session` с предупреждением; строка в Error handling; заметка в Assumptions

### Minor (Tier 3)

#### [M1] git ls-remote: грубый вердикт, интерактивные запросы
**Статус:** Автоисправлено — `GIT_TERMINAL_PROMPT=0` + `GIT_SSH_COMMAND='ssh -oBatchMode=yes'`, detail «did not answer (or refused)»

#### [M2] Code-review диапазон игнорирует dirty worktree
**Статус:** Автоисправлено — проверка `git status --porcelain` до записи промпта; предупреждение оператору + заметка в DOCUMENTS; молча не игнорируется

#### [M3] Асимметрия перезаписи prompt-файлов
**Статус:** Автоисправлено — правило `-2`/`-3` и для design-review генератора

#### [M4] Строка `provider` без двоеточия вне документированного порядка
**Статус:** Автоисправлено — агрегатная строка задокументирована в порядке строк дизайна и Global Constraints

#### [M5] Leak-scan слаб (regex, не покрывает пресеты)
**Статус:** Автоисправлено — `grep -qF`; скан дополнен `get-defaults` (`.claude_models[]`, `.models[]`; `.builtin[]` сознательно пропущен — «claude»/«codex» есть в любом промпте)

#### [M6] Нет ассерта согласованности SUMMARY с построчными вердиктами
**Статус:** Автоисправлено — membership-проверка каждой `provider:X`-строки против available/unavailable

#### [M7] Финальная тест-команда маскирует код возврата
**Статус:** Автоисправлено — FAILED-аккумулятор, ненулевой exit при падении любой сьюты

#### [M8] Тесты генераторов — не regression suite (нужен рендерер)
**Статус:** Отклонено — детерминированный рендерер противоречит идиоме плагина (команды — промпты); дрейф закрыт взаимными SYNC-заметками (I10); ужесточение ассертов уже в плане

#### [M9] Микротест ссылается на отсутствующий preflight-блок; скоринг одномерен
**Статус:** Автоисправлено — заглушка `## PREFLIGHT` в кандидате; push/PR-порывы — вторичные наблюдения; subagent-прогоны — прокси, приёмка = ручной ACCEPTANCE-прогон в реальной песочнице (записывается в baseline-файл)

#### [M10] SUMMARY-нейминг перегружен; «нечего маппить» неверно для claude-ревьюеров
**Статус:** Автоисправлено — SUMMARY разворачивает каталог как `claude:opus, claude:fable` (орфография confirm-страницы UI), plain `claude` только в fallback; decision 5, код Task 4 и тесты обновлены

#### [M11] `PROBED`-аккумулятор строкой вместо `declare -A`
**Статус:** Автоисправлено — `PROBED_STATUS` как ассоциативный массив (bash 4+ уже требуется)

#### [M12] Мелочи: мёртвые локалы, переполнение `%-16s`, потерянный exec-бит
**Статус:** Автоисправлено — локалы используются в новой версии; заметка «не чинить переполнение усечением имён» в Constraints; `[ -f ] && bash "$PF"`

#### [M13] Неточная цитата инцидента в Problem
**Статус:** Автоисправлено — 2026-07-26: четыре из шести без watchdog, 38 минут; 2026-07-27: четыре из пяти

#### [M14] README продаёт нишевый workflow как общую фичу
**Статус:** Автоисправлено — формулировка minimax + трёхшаговый пример workflow хост→песочница

### Suggestions (Tier 4)

#### [S1] Последовательная проба: 25–50 s
**Статус:** Обсуждено с пользователем. **Ответ:** Вариант B — прогресс-строки на stderr + `PREFLIGHT_SKIP_NETWORK=1` для повторных прогонов; параллель отложена в Assumptions («после стабилизации trap-контракта»). **Действие:** гейты во всех трёх сетевых зонах, тест-сценарий, дизайн-параграфы

#### [S2] Запретить генератору разворачивать `$HOME`
**Статус:** Автоисправлено — «emit `$HOME` literally» в обоих compose-интро с объяснением через decision 2

#### [S3] Передавать `TOPIC=` в GO-строке
**Статус:** Автоисправлено — снимает половину риска рассинхрона деривации (decision 4)

#### [S4] Endpoint в detail провайдерной строки
**Статус:** Автоисправлено — сабшелл возвращает `rc|base_url`; base_url не секрет; git-remote URL сознательно не печатается (может содержать креды)

#### [S5] Статус LIKELY для эвристик (+Q5)
**Статус:** Обсуждено с пользователем. **Ответ:** Вариант B — сноска в PREFLIGHT-секции промптов («OK у codex/gemini — эвристика, не auth; OK у gh/glab — только наличие»), закрытый статусный набор не вскрывается. **Действие:** сноска в дизайне и обоих шаблонах

#### [S6] Проба — единственный источник истины; snapshot, не монитор
**Статус:** Автоисправлено — параграф «Authority and lifetime» в дизайне

### Questions (Tier 5)

#### [Q1] Что печатать в SUMMARY available при config MISSING → решено в B1 (вариант A)
#### [Q2] Как гонять верификацию до переустановки → решено в I8 (исполнять файлы напрямую)
#### [Q3] Есть ли codex/gemini в целевой песочнице
**Статус:** Отклонено — probe гейтится на секцию конфига до сети: песочница без секций платит ~0 s, спецкейсить не нужно
#### [Q4] Верификационные артефакты удаляются из PR правилом docs/superpowers
**Статус:** Отклонено — работает как задумано: собственная конвенция проекта (git rm перед PR), история ветки сохраняет доказательства
#### [Q5] Что утверждает OK → решено в S5 (сноска; после B9 «OK» = секция валидна + CLI + эндпоинт)
#### [Q6] Покрывает ли запрет локальный merge → решено в I2 (да: finishing удаляет ветку)
#### [Q7] Источник CONTEXT итерации 1
**Статус:** Автоисправлено — источник только текущая сессия; нет контекста → спросить пользователя, не выдумывать; отказ → явная пометка о неполноте
#### [Q8] Shallow clone в песочнице
**Статус:** Отклонено — песочница монтирует рабочую копию (clone не участвует); диапазон резолвится на хосте; ревью-скиллы переопределяют базу сами
#### [Q9] Как do-plan-сессия узнаёт, что она в песочнице
**Статус:** Автоисправлено — не гадать: `GIT_TERMINAL_PROMPT=0 timeout 8 git ls-remote` либо переиспользовать вердикт пробы из этой сессии

### Ложные срабатывания (Tier 6)

#### [F1] «via the Skill tool некорректно для command-файлов» (kimi Critical 3)
**Статус:** Отклонено — опровергнуто самим executor'ом: command-файлы вызываются через Skill tool в текущем Claude Code
#### [F2] Описание опции Step 15 станет неверным (minimax Critical 1)
**Статус:** Отклонено — самоопровергается в собственном абзаце («текст опции остаётся корректным»)
#### [F3] Fallback-цепочка не включает main (kimi Q2)
**Статус:** Отклонено — отвечено в коде: автодетект `origin/HEAD` покрывает оба имени (iter-2 CONCERN-9 в ext-claude-code-review); shallow-половина вопроса → Q8
#### [F4] `list-claude-models`/`has_claude_models` могут не существовать (deepseek Critical 1)
**Статус:** Отклонено — существуют (PR #9, e640ef1); подтверждено executor'ом
#### [F5] Мета-вердикт qwen «критических проблем нет»
**Статус:** Отклонено как отдельное замечание — секция дала I11, части M5/S1/M14

## Изменения в документах

| Файл | Изменение |
|------|-----------|
| `...-design.md` | Problem (точная цитата инцидента); decision 2 (двухступенчатый discovery + оговорка --plugin-dir); decision 4 (правило TOPIC дословно + поиск плана); decision 5 (claude:<model>-орфография, typed-getter гейты); decision 6 и Error handling (честная деградация после B1); шаблон промпта (PREFLIGHT-блок exit 0/bash, сноска об эвристиках, TOPIC= и default-правило в GO); хвосты генераторов (источник CONTEXT, slug, dirty-check); секция пробы (identity-строка, toolchain, ext-claude-deps, SKIP_NETWORK, честный Cost, SUMMARY-блок с defaults, Authority and lifetime); Testing (позитивный критерий, ACCEPTANCE); Assumptions (отложенная параллель, missing-command деградация) |
| `...-plan.md` | Global Constraints (порядок строк, exit-контракт, все PREFLIGHT_*-переменные, default-правило); File Structure (+2 файла); Task 1 (run()-идиома, curl-шим, identity/toolchain/prerequisites, rc-aware claude-models, B6-гейт, новые сценарии); Task 2 (probe_provider через глобалы, rc|base_url, stderr-routing export, PROBED_STATUS, гейты ext-deps/curl/skip-net, прогресс, env-ручки ollama-precheck); Task 3 (typed-getter в cli_row, git-hardening, INVALID-сценарий); Task 4 (SUMMARY: claude:<m>, гейт B1, defaults-строки, hint, consistency-ассерт); Task 5 (изоляция, пороги, заглушка PREFLIGHT); Task 6/7 (sync-notes, $HOME literally, коллизии, slug, dirty, прямое исполнение файлов, GREEN-изоляция ×5 критериев); Task 8 (порядок finishing, fallback старого плагина, встречные SYNC, операционный sandbox-чек); Task 9 (README-пример, rc-пропагация) |

## Статистика

- Всего замечаний: 57 (сырых ~120, дедуплицировано review-discussion)
- Автоисправлено (без обсуждения): 36
- Авто-применено после анализа: 4 (B6, I2, I8, I9)
- Обсуждено с пользователем: 4 (B1→A; B12→B+C; S1→B; S5→B; вопросы Q1/Q2/Q5/Q6 свёрнуты в свои темы)
- Отклонено (ложные/отвечено): 9 (F1–F5, M8, Q3, Q4, Q8)
- Повторов (автоответ): 0
- Отложено (стоп): 0
- Пользователь сказал «стоп»: Нет
- Агенты: claude:opus, claude:fable, codex, zai/glm, alibaba/qwen, deepseek/v4-pro, ollama/kimi, ollama/minimax + review-discussion (opus)
